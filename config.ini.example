; 데이터베이스 설정 (이 파일을 config.ini로 복사하여 사용하세요)
; 키 이름은 `includes/db.php`에서 기대하는 값과 동일합니다.
; 로컬(XAMPP) 예시
DB_HOST = localhost
DB_USER = root
DB_PASS = 
DB_NAME = placelog

; 운영(InfinityFree) 예시 — 필요 시 아래 값을 사용하세요
;DB_HOST = sql100.infinityfree.com
;DB_USER = if0_40576741
;DB_PASS = your_db_password_here
;DB_NAME = if0_40576741_placelog
